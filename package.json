{
    "name": "react-and-rails",
    "scripts": {
      "build": "cd top-marks && yarn install && yarn run build && cd ..",
      "deploy": "cp -a top-marks/build/. public/",
      "postinstall": "yarn run build && yarn run deploy && echo 'Client built!'"
    }
  }